using UnityEngine;

public class LanguageManager : MonoBehaviour
{
    

    /// <summary>
    /// Returns the translated string based on the chosen application language.
    /// If the string doesn't exist in the dictionary, it returns the same string value.
    /// </summary>
    /// <param name="phrase"></param>
    /// <returns></returns>
    public static string Translate(string phrase = "")
    {
        /********language modile that translate the input string*******/
        return phrase;
    }

}